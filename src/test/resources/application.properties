# Solo aplica a los tests de integración con Testcontainers.
# El datasource real lo inyecta PostgresTestContainer via @DynamicPropertySource.
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
